<!DOCTYPE html>
<html lang="en">
<head><%- include('../partials/head') %></head>
<body class="container">

<header>
    <%- include('../partials/header') %>
</header>

<main class="mt-3 mb-3">
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header">
                    <h4 class="float-left"><%= title %></h4>
                    <div class="float-right">
                        <a href="/service-provider/update/<%= serviceProvider._id %>" class="btn btn-info">Update</a>
                        <a href="/service-provider/delete/<%= serviceProvider._id %>" class="btn btn-danger">Delete</a>
                    </div>
                </div>
                <div class="card-body">
                    <table class="table table-bordered">
                        <tbody>
                            <tr>
                                <th scope="col">Name</th>
                                <td><%= serviceProvider.name %></td>
                            </tr>
                            <tr>
                                <th scope="col">Mobile</th> 
                                <td><%= serviceProvider.mobile %></td>
                            </tr>
                            <tr>
                                <th scope="col">Email</th>
                                <td><%= serviceProvider.email %></td>
                            </tr>
                            <tr>
                                <th scope="col">Service Charge</th>
                                <td><%= serviceProvider.serviceCharge %></td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>    
</main>

<footer>
    <%- include('../partials/footer') %>
</footer>

</body>
</html>